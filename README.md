 Классификация видов птиц CUB-200-2011 с ResNet18

Проектный практикум МИФИ 2025
Автор: Смирнов Сергей

 Описание проекта

Цель проекта — разработать прототип системы автоматической классификации видов птиц по их изображениям с использованием предобученных моделей компьютерного зрения.

В работе используется датасет CUB-200-2011, содержащий:

200 классов птиц

более 6000 изображений

Модель решает следующие задачи:

предсказание вида птицы по изображению;

вывод вероятностей top-1 и top-5;

анализ ошибок с помощью матрицы путаницы (confusion matrix);

визуализация предсказаний и обучение модели с прогресс-баром.

Прототип может применяться в системах мониторинга экосистем:

наблюдение за биоразнообразием;

оценка динамики популяций;

автоматическая обработка данных с фотоловушек и камер наблюдения.

 Структура проекта
├── README.md                  # описание проекта
├── cub-bird-classification.ipynb  # основной Jupyter-ноутбук
├── /notebooks                 # доп. ноутбуки (опционально)
└── /models                    # сохр. модели (опционально)

 Используемая модель

В проекте применяется предобученная модель:

ResNet18 (ImageNet pretrained)

Оптимизации:

заморозка всех слоёв кроме последнего (feature extractor);

fine-tuning последнего полносвязного слоя под 200 классов;

ускоренное обучение на подвыборке;

аугментации (RandomHorizontalFlip);

нормализация под ImageNet;

вычисление top-1 и top-5 accuracy.

🔧 Установка и запуск
1. Установка зависимостей
pip install torch torchvision tqdm scikit-learn matplotlib seaborn

2. Запуск ноутбука

Открыть:

cub-bird-classification.ipynb


Ноутбук полностью автономный:
он скачивает датасет CUB-200-2011, распаковывает его и запускается без дополнительной подготовки.

🏋️ Обучение модели

В ноутбуке реализованы:

прогресс-бар обучения (tqdm);

логирование loss/accuracy;

отдельные функции для train/valid/test циклов;

визуализация батчей;

построение confusion matrix для 200 классов;

визуализация top-20 классов.

Пример обучения на подвыборке:

num_epochs = 3
train_one_epoch(...)
evaluate(...)

 Результаты

Обучение выполнено на подвыборке для ускорения.

 Метрики на тестовой выборке:
Метрика	Значение
Test loss	3.4962
Top-1 accuracy	24.20%
Top-5 accuracy	53.60%

Эти значения соответствуют модели, обученной на ограниченном количестве данных (500–2000 примеров). При увеличении обучающей выборки метрики растут значительно.

 Анализ ошибок

Ноутбук автоматически строит:

полную confusion matrix (200×200) — для общей картины ошибок;

топ-20 самых частых классов — для удобочитаемой визуализации;

отображение предсказаний модели с вероятностями top-5.

🧪 Демонстрация предсказания

В ноутбуке есть функция:

predict_image(path, model, class_names, device)


Она выводит картинку и top-5 вероятностей.

 Выводы

Разработан рабочий прототип системы классификации птиц на базе ResNet18.
Он демонстрирует:

корректное применение предобученных моделей,

целостный ML-pipeline,

работу с реальным набором данных,

построение метрик качества и визуализацию ошибок.

Модель может быть расширена:

использованием большего объёма данных,

fine-tuning всех слоёв ResNet,

заменой архитектуры на ResNet50 / EfficientNet / ViT,

добавлением обучения новым видам птиц.
